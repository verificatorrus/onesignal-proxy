{
  "private": true,
  "name": "onesignal-proxy",
  "version": "0.0.1",
  "description": "Proxy for OneSignal service on Cloudflare Workers. Resolve OneSignal AD blockers problem.",
  "main": "index.js",
  "scripts": {
    "publish": "wrangler publish",
    "preview": "wrangler preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Egor Zudin <verificatorrus@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
